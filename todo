- we need to make folder structure for the http backend use the mvc pattern for express
- follow better linitng principles with proppe types 
- send the token in the web socket through an header and not a params format 